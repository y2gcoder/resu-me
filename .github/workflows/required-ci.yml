name: required-ci
on:
  pull_request:
jobs:
  required-ci:          # job id
    name: required-ci   # reported check name
    runs-on: ubuntu-latest
    steps:
      - run: echo "Always-on required check ✅"
